---
description: Recover earlier project work with Tang and continue it here safely
---

Load the `tang` skill and follow it exactly for this recovery workflow. If the skill is unavailable, stop and tell the user to run `tang skill install opencode --project-root "$PWD"` and restart OpenCode; do not substitute a repository file or another harness's skill.
