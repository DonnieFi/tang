# Canonical Multiverse graph fixture

Synthetic, deterministic data only. The connected component encodes the explicit
many-to-many topology A/B→C, C→D/E, E/F→G, with G as the current Codex handle.
Node H is isolated and F represents an unavailable native source retained by a
confirmed edge. Invalid self and cycle candidates are test inputs, never edges.
